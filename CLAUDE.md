# bwraps — Project Entry

Marketing site for **B Printing and Wraps**, a print/wrap/embroidery shop in **Surprise, AZ**.
Building a demo for the owner (Wyatt's friend) to review. Reference/old site: bwraps1.com.
Static HTML/CSS/JS generated by one Python script — same engine as the `anderson-it` project.

## ⭐ LATEST SESSION — 2026-07-24 (SEO, chat lead-gen, hero rewrite, services grid — LIVE, CSS v26, chat.js v5)
Big day, all shipped live: (1) **hero badge drop-down** (CSS v21); (2) **full SEO** — canonical + OG +
Twitter Card + **LocalBusiness JSON-LD** (5.0/20 aggregateRating, hours, address, sameAs, hasMap) + richer
sitemap (CSS v22); (3) **center everything on mobile ≤600px** — all 5 pages verified `scrollWidth==390`;
(4) **chat nudge + lead capture** (CSS v23, chat.js v3) — proactive once/session nudge, full **transcript
emailed with every lead**, and an **intent-triggered follow-up** (after a price keyword or 2 turns → collect
name + email/phone → email transcript); Worker `/lead` now takes email-OR-phone + a Conversation field
(**needs `wrangler deploy` at cutover**); (5) **hero rewrite** — H1 now "Printing, wraps & embroidery that
make your brand impossible to miss", trimmed subhead, new maps-linked **"Come see us in person"** address+hours
block (CSS v24–25); (6) **chat status-dot fix** (was a collapsed green sliver → `inline-block`); (7) **services
page core = 2-up `.prod-grid`** of product cards, collapses to 1-col ≤600, section ids preserved (CSS v26);
(8) **wizard name-step wording** → "Now let's grab your contact info. What's your name?" (chat.js v5). Full
detail + evidence + decisions in [docs/SESSION_STATE_2026-07-24.md](docs/SESSION_STATE_2026-07-24.md).
Gotchas: HTML isn't cache-busted (hard-refresh); Browser pane flaky (drive chat via javascript_tool, verify
via forced-width scrollWidth + computed styles); dead `.svc-row-*` CSS left for later cleanup. Owner still
owes: FormSubmit activation, GoDaddy nameserver swap, reviewer names, Google-listing phone fix.

## ⭐ PRIOR SESSION — 2026-07-23 (theme v2 — LIVE, CSS v19, 39 gallery tiles)
Full rebuild + **deployed live: https://wyatt741.github.io/bwraps/** (dark-mode default). This session:
pink+ink palette (from the real logo), dark-mode toggle, **demo chatbot** (`chat.js`), real Google reviews,
animations + **animated CTAs**, high-end-visual-design pass (line icons, double-bezel, grain), expanded
services/gallery copy, a **mobile-layout pass** (fixed contact-form overflow), **Contact in header** +
single toggle, and a **full Google Business Profile photo pull** (via signed-in Chrome, owner permission
confirmed) — added **23 real client photos → gallery now 39 tiles**, reviews stat bumped to **50+**.
Then a polish round: eyebrow contrast + white dark text, header wordmark (cropped B mark + real type), and a
batch of real **mobile-overflow bug fixes** (hero padding-shorthand zeroing side padding; brands-marquee blowing
out page width; chat scroll-chaining; hero centered on mobile). **FormSubmit activation TRIGGERED** — owner must
click the confirm link in their inbox to finish. Repo **PUBLIC** for Pages; `docs/` gitignored. Full detail +
gotchas (HTML-not-cache-busted → hard-refresh; Google photo-pull; **how to actually measure mobile overflow** —
force width, the flaky pane lies about viewport) in [docs/SESSION_STATE_2026-07-23.md](docs/SESSION_STATE_2026-07-23.md).
**Owner still owes:** click the FormSubmit activation link, real reviewer names, confirm/trim brands, and
**fix the Google-listing phone** (shows 480-261-2133; correct is 928-230-8525, already on the site).
**DNS:** Cloudflare zone bwraps1.com created (Free, records DNS-only, Duda still live) — **PENDING GoDaddy
nameserver swap to `cris.ns.cloudflare.com` + `naya.ns.cloudflare.com`** (needs owner's GoDaddy login).
New-site cutover is prepped: `bash docs/cutover.sh` (local) once Path-B DNS records point at GitHub.
**AI chatbot:** hybrid bot deployed + TESTED (Cloudflare Worker `bwraps-chat`, in `worker/`) — free-text=AI,
wizard=deterministic. Parked in DEMO mode; activate at cutover via `chat.bwraps1.com` + `WORKER_URL`.
**site-template** (separate repo) is now a real scaffold built from this site.

## Files
| Path | Purpose |
|------|---------|
| `build.py` | Emits every page (index/services/gallery/about/contact) + sitemap/robots. Icons/FEATURES/BRANDS near top. Edit content HERE, run `python3 build.py`. Never hand-edit generated HTML. Cache vers CSSV/JSV/CHATV. |
| `styles.css` / `app.js` | Theme v2: variable-swap dark/light, glass nav, photo tiles, marquee, double-bezel icons, grain, animations + behavior (menu, reveals, gallery filter, lightbox, theme toggle). Cache-busted `?v=N`. |
| `chat.js` | Demo chat assistant — client-side quote wizard + keyword answers (no backend). |
| `assets/` | 26 real work photos + 4 brand logos + 2 logos (all real, tracked). |
| `docs/CREATIVE_BRIEF.md` | The structuring brief. **LOCAL ONLY (gitignored — repo is public).** |

## Conventions (carried from anderson-it)
- No fabricated reviews/stats. Testimonials are SAMPLE-marked until real ones arrive.
- Concise voice, no em dashes. Playful/local tone fits this brand.
- Contact form = FormSubmit.co → elitecustomprinting@outlook.com (needs owner activation).
- Preview: `python3 -m http.server 8731 --directory .` then the Browser pane (navigate → screenshot immediately; pane is flaky on scroll).
- Deploy: `git push origin main` → GitHub Pages auto-rebuilds. Repo is **PUBLIC** — no internal notes/emails in tracked files.

## Status
Theme v2 built + **LIVE** at https://wyatt741.github.io/bwraps/ (dark default). Owner-side to-dos before it's
their real site: activate FormSubmit inbox, add real reviewer names, confirm/trim brands, point custom domain.

## Resume
Read `docs/CREATIVE_BRIEF.md`. Say `resume bwraps`.
