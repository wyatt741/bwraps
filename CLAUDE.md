# bwraps — Project Entry

Marketing site for **B Printing and Wraps**, a print/wrap/embroidery shop in **Surprise, AZ**.
Building a demo for the owner (Wyatt's friend) to review. Reference/old site: bwraps1.com.
Static HTML/CSS/JS generated by one Python script — same engine as the `anderson-it` project.

## ⭐ LATEST SESSION — 2026-07-23
Demo v1 built (5 pages, bold colorful theme, gallery filter, quote form) with placeholders.
Real assets + socials pulled from bwraps1.com and captured in
[docs/SESSION_STATE_2026-07-23.md](docs/SESSION_STATE_2026-07-23.md) — **exact next step: download
those real images into `assets/`, fix TikTok to @luckstitch, wire in the real logo/photos.**
Owner still owes: real form inbox, real testimonials, more work photos.

## Files
| Path | Purpose |
|------|---------|
| `build.py` | Emits every page (index/services/gallery/about/contact) + sitemap/robots. Edit content HERE, run `py build.py`. Never hand-edit generated HTML. |
| `styles.css` / `app.js` | Bold colorful theme + behavior (mobile menu, reveals, gallery filter). Cache-busted `?v=N` (`CSSV`/`JSV` in build.py). |
| `assets/` | Real logo + work photos go here (owner to supply). |
| `docs/CREATIVE_BRIEF.md` | The structuring brief — read first. |

## Conventions (carried from anderson-it)
- No fabricated reviews/stats. Testimonials are SAMPLE-marked until real ones arrive.
- Concise voice, no em dashes. Playful/local tone fits this brand.
- Contact form = FormSubmit.co (needs the owner's real inbox + activation).
- Preview: `py -m http.server 8731 --directory .` then open the Browser pane.
- Deploy (once approved): git + GitHub Pages, like anderson-it.

## Status
Demo v1 built with placeholders. Pending from owner: logo, work photos, real form email,
confirm TikTok handle, real testimonials. Then deploy to a shareable URL.

## Resume
Read `docs/CREATIVE_BRIEF.md`. Say `resume bwraps`.
